# Ieșiri MD — Spec (MVP)
Curated weekend ideas Chișinău: seed 16–20 locuri (parc, muzeu, cafe, plimbare) cu tag (gratis/ieftin), zonă, scurtă descriere.
Filter by tag/zone, favorites in localStorage. RO/RU. Next.js 16 + shadcn + GSAP.
Routes: `/`, `/place/[id]` or detail sheet, `/favorites`, `/settings`. No backend. `npm run build`=0. Push ndolinschi/iesiri-md.
